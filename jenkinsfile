pipeline {
    agent any
    stages {
       
        stage ('Build') {
            
            steps {
                 withMaven(maven: 'MAVEN_HOME') {
                 bat "mvn -f path/to/pom.xml clean install"
                 bat "mvn -f path/to/pom.xml clean package"
                 }
           }
        }
    }
}
